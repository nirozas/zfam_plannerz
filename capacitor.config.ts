import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zoabi.planner.nexus',
  appName: 'Zoabi Nexus Vault',
  webDir: 'dist'
};

export default config;
